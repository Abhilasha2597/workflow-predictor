services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      OpenAI__ApiKey: ${OPENAI_API_KEY:-}
      OpenAI__Model: ${OPENAI_MODEL:-gpt-4.1-mini}
      GitHub__Token: ${GITHUB_TOKEN:-}
